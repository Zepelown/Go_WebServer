# 바이너리 및 실행 파일
# go build로 생성되는 결과물은 포함하지 않습니다.
*.exe
*.exe~
*.dll
*.so
*.dylib

# 프로젝트 이름과 동일한 이름으로 생성되는 Linux/macOS 실행 파일
# 예: 프로젝트 폴더 이름이 'my-app'이면, 'my-app'이라는 실행 파일을 무시합니다.
# 이 부분은 프로젝트 이름에 맞게 수정하거나, 빌드 결과물 경로를 지정하는 것이 더 좋습니다.
# my-app 

# 테스트 관련 파일
# go test -c 로 생성되는 테스트 바이너리
*.test

# go tool cover 로 생성되는 커버리지 프로필
*.out


# 의존성 디렉터리
# Go 모듈을 사용하므로, vendor 디렉터리는 보통 포함하지 않습니다.
# 필요하다면 아래 줄의 주석을 해제하세요.
# vendor/


# 환경 변수 파일 (매우 중요!)
# 민감한 정보가 담긴 .env 파일은 절대로 커밋하면 안 됩니다.
.env


# OS 및 에디터 관련 파일
# macOS
.DS_Store

# Windows
Thumbs.db

# VS Code
.vscode/

# GoLand
.idea/
